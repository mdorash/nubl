<head>

<h1><%= yield :title %></h1>
</head>
<body>
<div class="container">
  <div class="row">
    <div class="span3">
      <div class="span10">
    <% @posts.each do |post| %>
    <table class="table">
      <tbody>
      <tr>
        <dl><dt><%= link_to post.title, post.url %>   </dt>
        <dd> <%= link_to "Рэдагаваць", edit_post_path(post)%>
    <%= link_to 'Выдаліць', post, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
    <%= link_to "Каментары", new_comment_path(:my => post.id) %>
    <%= link_to'Падабаецца', post_like_path(post) %> <%= post.rating %> </dd>   </dl>
      </tr>

      </tbody>
    </table>

  <% end %>

<p>
  <a href="http://localhost:3000/posts/new" class="btn btn-info ">стварыць пост</a>
  </p>
    </div>
    </div>
</div>
</div>
</body>

